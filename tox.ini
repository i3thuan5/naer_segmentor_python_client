[testenv:試驗]
deps =
    python-coveralls
commands =
    coverage run --source=naer,test -m unittest discover
    coverage report
    coveralls

[testenv:flake8]
deps =
    flake8
commands =
	flake8 . --exclude=venv,.tox --count --show-source
